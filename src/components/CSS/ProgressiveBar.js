import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  labelText: {
    fontSize: 13,
    color: "white"
  },
  questionText: { 
    color: "white", 
    fontSize: 20, 
    textAlign: 'center', 
    position: 'relative', 
    top: 20 
  }
})